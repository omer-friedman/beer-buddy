import styled from 'styled-components';

const RankIon = styled.div`
    color:#FE7;
    text-shadow: 0 0 20px #952;

    &:hover{
        transform: rotate(-15deg);
    }
`
export default RankIon
